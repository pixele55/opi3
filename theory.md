# Теоретическая часть — ЛР 5
**Студент:** [Чернов Дмитрий Андреевич]  
**Student ID:** 22  
**Вариант Q3:** 1 (Sigmoid + Tanh)  
**Архитектура:** 784 → 256 → 128 → 10  

---

## Q1. Прямой проход

### 1.1 Формулы прямого прохода для батча

Пусть у нас есть батч из $N$ примеров. Обозначим:

-   $X = a^{(0)} \in \mathbb{R}^{N \times n_0}$ — входная матрица батча ($n_0 = 784$).
-   $W^{(1)} \in \mathbb{R}^{n_0 \times n_1}$ — веса первого слоя ($n_1 = 256$).
-   $b^{(1)} \in \mathbb{R}^{n_1}$ — смещения первого слоя (транслируются на всю строку).
-   $W^{(2)} \in \mathbb{R}^{n_1 \times n_2}$ — веса второго слоя ($n_2 = 128$).
-   $b^{(2)} \in \mathbb{R}^{n_2}$.
-   $W^{(3)} \in \mathbb{R}^{n_2 \times K}$ — веса выходного слоя ($K = 10$).
-   $b^{(3)} \in \mathbb{R}^{K}$.

**Прямой проход:**

1.  **Скрытый слой 1 (Sigmoid):**
    $$Z^{(1)} = a^{(0)} W^{(1)} + b^{(1)} \quad \text{(размерность: } N \times n_1\text{)}$$
    $$a^{(1)} = \sigma_{\text{sigmoid}}(Z^{(1)}) = \frac{1}{1 + \exp(-Z^{(1)})} \quad \text{(применяется поэлементно)}$$

2.  **Скрытый слой 2 (Tanh):**
    $$Z^{(2)} = a^{(1)} W^{(2)} + b^{(2)} \quad \text{(размерность: } N \times n_2\text{)}$$
    $$a^{(2)} = \sigma_{\text{tanh}}(Z^{(2)}) = \frac{e^{Z^{(2)}} - e^{-Z^{(2)}}}{e^{Z^{(2)}} + e^{-Z^{(2)}}} \quad \text{(поэлементно)}$$

3.  **Выходной слой (Softmax):**
    $$Z^{(3)} = a^{(2)} W^{(3)} + b^{(3)} \quad \text{(размерность: } N \times K\text{)}$$
    $$\hat{Y} = \text{softmax}(Z^{(3)}), \quad \hat{y}_{ik} = \frac{\exp(z^{(3)}_{ik})}{\sum_{j=1}^{K} \exp(z^{(3)}_{ij})}$$

### 1.2 Функция потерь Cross-Entropy

Для одного примера $(\mathbf{x}_i, y_i)$ с one-hot кодированием метки $y_i$ (вектор $y_i \in \{0,1\}^K$, где 1 только на позиции истинного класса) функция потерь определяется как:

$$\mathscr{L}(\hat{y}_i, y_i) = -\sum_{k=1}^{K} y_{ik} \log \hat{y}_{ik}$$

**Роль one-hot кодирования:** Сумма по $k$ содержит $K-1$ нулевых слагаемых (где $y_{ik}=0$) и одно ненулевое (где $y_{ik}=1$). Таким образом, лосс упрощается до $-\log \hat{y}_{i, \text{true}}$. Штрафуется только уверенность сети в правильном классе.

**Стремление к нулю:** Если сеть классифицирует пример верно с высокой уверенностью, то вероятность правильного класса $\hat{y}_{i, \text{true}} \to 1$. Следовательно, $\log(1) = 0$, и $\mathscr{L} \to 0$. Функционал качества $Q$ — это среднее арифметическое лоссов по всей выборке $X^{\ell}$:
$$Q(\boldsymbol{\theta}, X^\ell) = \frac{1}{\ell}\sum_{i=1}^{\ell} \mathscr{L}(\hat{y}_i, y_i)$$

### 1.3 Численная стабильность Softmax

Прямое вычисление $\frac{\exp(z_k)}{\sum_{j}\exp(z_j)}$ нестабильно при больших $z_k$, так как $\exp(z_k)$ стремится к бесконечности, вызывая переполнение.

**Стабильная формула:** вычтем максимальное значение логита $M = \max_j z_j$:
$$\hat{y}_k = \frac{\exp(z_k - M)}{\sum_{j=1}^{K} \exp(z_j - M)}$$

**Доказательство эквивалентности:**
$$\hat{y}_k = \frac{\exp(z_k)/\exp(M)}{\sum_{j}\exp(z_j)/\exp(M)} = \frac{\exp(z_k)/\exp(M)}{\left(\sum_{j}\exp(z_j)\right)/\exp(M)} = \frac{\exp(z_k)}{\sum_{j}\exp(z_j)}$$
Так как $\exp(M) > 0$, мы можем разделить числитель и знаменатель на эту константу. В стабильной версии все показатели экспоненты $\le 0$, что предотвращает переполнение.

---

## Q2. Обратный проход и метод обратного распространения ошибки

### 2.1 Градиент для Softmax + Cross-Entropy

Рассмотрим среднюю ошибку по батчу $Q = \frac{1}{N} \sum_{i=1}^{N} \mathscr{L}_i$, где $\mathscr{L}_i = -\sum_{k=1}^{K} y_{ik} \log \hat{y}_{ik}$.

Найдем $\frac{\partial Q}{\partial z^{(3)}_{ij}}$. Сначала для одного примера. Производная softmax: $\frac{\partial \hat{y}_{im}}{\partial z^{(3)}_{ij}} = \hat{y}_{im}(\delta_{mj} - \hat{y}_{ij})$, где $\delta_{mj}=1$ при $m=j$, иначе $0$.

$$\frac{\partial \mathscr{L}_i}{\partial z^{(3)}_{ij}} = -\sum_{m=1}^{K} \frac{y_{im}}{\hat{y}_{im}} \cdot \frac{\partial \hat{y}_{im}}{\partial z^{(3)}_{ij}} = -\sum_{m} \frac{y_{im}}{\hat{y}_{im}} \hat{y}_{im}(\delta_{mj} - \hat{y}_{ij}) = -\sum_{m} y_{im} (\delta_{mj} - \hat{y}_{ij})$$

$$= -\left( \sum_{m} y_{im} \delta_{mj} - \sum_{m} y_{im} \hat{y}_{ij} \right) = - (y_{ij} - \hat{y}_{ij} \sum_{m} y_{im}) = - (y_{ij} - \hat{y}_{ij} \cdot 1) = \hat{y}_{ij} - y_{ij}$$

Усредняя по батчу: $\frac{\partial Q}{\partial z^{(3)}_{ij}} = \frac{1}{N} (\hat{y}_{ij} - y_{ij})$. В матричной форме, где $\hat{Y}, Y \in \mathbb{R}^{N \times K}$:

$$\boxed{\frac{\partial Q}{\partial Z^{(3)}} = \frac{1}{N}(\hat{Y} - Y)}$$

### 2.2 Градиенты для выходного слоя

Используем правило цепочки. Так как $Z^{(3)} = a^{(2)} W^{(3)} + b^{(3)}$:

-   Для весов: $\frac{\partial Q}{\partial W^{(3)}_{pq}} = \sum_{i=1}^{N} \frac{\partial Q}{\partial Z^{(3)}_{iq}} \cdot \frac{\partial Z^{(3)}_{iq}}{\partial W^{(3)}_{pq}}$. Поскольку $\frac{\partial Z^{(3)}_{iq}}{\partial W^{(3)}_{pq}} = a^{(2)}_{ip}$, то в матричной форме:
    $$\boxed{\frac{\partial Q}{\partial W^{(3)}} = (a^{(2)})^{\top} \cdot \frac{\partial Q}{\partial Z^{(3)}}}, \quad \text{где } a^{(2)} \in \mathbb{R}^{N \times n_2}, \frac{\partial Q}{\partial Z^{(3)}} \in \mathbb{R}^{N \times K} \Rightarrow \frac{\partial Q}{\partial W^{(3)}} \in \mathbb{R}^{n_2 \times K}$$

-   Для смещений: $\frac{\partial Q}{\partial b^{(3)}_{q}} = \sum_{i=1}^{N} \frac{\partial Q}{\partial Z^{(3)}_{iq}} \cdot 1$. Суммируем градиенты по батчу:
    $$\boxed{\frac{\partial Q}{\partial b^{(3)}} = \sum_{i=1}^{N} \left(\frac{\partial Q}{\partial Z^{(3)}}\right)_{i} \in \mathbb{R}^{K}}$$

### 2.3 Общая формула обратного распространения (дельта-правило)

Для скрытого слоя $l$ определим дельту $\delta^{(l)} = \frac{\partial Q}{\partial Z^{(l)}} \in \mathbb{R}^{N \times n_l}$.

**Вывод:** $Z^{(l)} = a^{(l-1)} W^{(l)} + b^{(l)}$, и $a^{(l)} = \sigma_l(Z^{(l)})$. Далее $Z^{(l+1)} = a^{(l)} W^{(l+1)} + b^{(l+1)}$.

По цепному правилу для сложной функции: сначала ошибка распространяется от $Z^{(l+1)}$ к $a^{(l)}$, а затем от $a^{(l)}$ к $Z^{(l)}$.

1.  Влияние $a^{(l)}$ на $Z^{(l+1)}$: $\frac{\partial Z^{(l+1)}}{\partial a^{(l)}} = W^{(l+1)}$.
2.  Влияние $Z^{(l)}$ на $a^{(l)}$: $\frac{\partial a^{(l)}}{\partial Z^{(l)}} = \sigma'_l(Z^{(l)})$ (поэлементно).

Таким образом:
$$\delta^{(l)} = \frac{\partial Q}{\partial Z^{(l)}} = \frac{\partial Q}{\partial Z^{(l+1)}} \cdot \frac{\partial Z^{(l+1)}}{\partial a^{(l)}} \odot \frac{\partial a^{(l)}}{\partial Z^{(l)}} = \left( \delta^{(l+1)} \cdot (W^{(l+1)})^{\top} \right) \odot \sigma'_l(Z^{(l)})$$

-   **$(W^{(l+1)})^{\top}$** нужна для транспонирования размерностей: $\delta^{(l+1)} \in \mathbb{R}^{N \times n_{l+1}}$, а $W^{(l+1)} \in \mathbb{R}^{n_l \times n_{l+1}}$, тогда $(W^{(l+1)})^{\top} \in \mathbb{R}^{n_{l+1} \times n_l}$.
-   **$\sigma'_l(Z^{(l)})$** — это производная функции активации, которая показывает, насколько сильно изменение преактивации $Z^{(l)}$ повлияет на пост-активацию $a^{(l)}$.

### 2.4 Вывод дельт и градиентов для скрытых слоев

**Дано:** $\delta^{(3)} = \frac{\partial Q}{\partial Z^{(3)}} = \frac{1}{N}(\hat{Y} - Y)$.

**Слой 2 (Tanh):**
$$\delta^{(2)} = \left( \delta^{(3)} \cdot (W^{(3)})^{\top} \right) \odot \sigma'_{\text{tanh}}(Z^{(2)})$$
Производная Tanh: $\sigma'_{\text{tanh}}(z) = 1 - \tanh^2(z) = 1 - (a^{(2)})^2$.

Градиенты:
$$\frac{\partial Q}{\partial W^{(2)}} = (a^{(1)})^{\top} \cdot \delta^{(2)}, \quad \frac{\partial Q}{\partial b^{(2)}} = \sum_{i=1}^{N} \delta^{(2)}_i$$

**Слой 1 (Sigmoid):**
$$\delta^{(1)} = \left( \delta^{(2)} \cdot (W^{(2)})^{\top} \right) \odot \sigma'_{\text{sigmoid}}(Z^{(1)})$$
Производная Sigmoid: $\sigma'_{\text{sigmoid}}(z) = \sigma_{\text{sigmoid}}(z) \cdot (1 - \sigma_{\text{sigmoid}}(z)) = a^{(1)} \odot (1 - a^{(1)})$.

Градиенты:
$$\frac{\partial Q}{\partial W^{(1)}} = (a^{(0)})^{\top} \cdot \delta^{(1)}, \quad \frac{\partial Q}{\partial b^{(1)}} = \sum_{i=1}^{N} \delta^{(1)}_i$$

---

## Q3. Функции активации и затухание градиентов

### 3.1 Определения и производные

**Sigmoid:**
$$\sigma_{\text{sigm}}(z) = \frac{1}{1 + e^{-z}}$$

*Вывод производной:*
$$\sigma'(z) = \frac{d}{dz} (1+e^{-z})^{-1} = -1 \cdot (1+e^{-z})^{-2} \cdot (-e^{-z}) = \frac{e^{-z}}{(1+e^{-z})^2}$$
Вынося $e^{-z}$ за скобки или, что удобнее, заменив $e^{-z} = \frac{1+e^{-z} - 1}{1} = \frac{1}{1+e^{-z}} - \frac{1}{1+e^{-z}}?$ Нет. Лучше:
$$\sigma'(z) = \frac{e^{-z}}{(1+e^{-z})^2} = \frac{1}{1+e^{-z}} \cdot \frac{e^{-z}}{1+e^{-z}} = \sigma(z) \cdot (1 - \sigma(z))$$

**Tanh:**
$$\sigma_{\text{tanh}}(z) = \frac{e^{z} - e^{-z}}{e^{z} + e^{-z}}$$

*Вывод производной (доказательство $\tanh'(z) = 1 - \tanh^2(z)$):*
$$\sigma'(z) = \frac{(e^{z} + e^{-z})^2 - (e^{z} - e^{-z})^2}{(e^{z} + e^{-z})^2}$$
Разность квадратов в числителе: $(A+B)^2 - (A-B)^2 = 4AB$, где $A=e^{z}, B=e^{-z}$.
$$= \frac{4 e^{z} e^{-z}}{(e^{z} + e^{-z})^2} = \frac{4}{(e^{z} + e^{-z})^2}$$
Теперь выразим через $\sigma_{\text{tanh}}(z)$:
$$1 - \sigma_{\text{tanh}}^2(z) = 1 - \left(\frac{e^{z} - e^{-z}}{e^{z} + e^{-z}}\right)^2 = \frac{(e^{z} + e^{-z})^2 - (e^{z} - e^{-z})^2}{(e^{z} + e^{-z})^2} = \frac{4}{(e^{z} + e^{-z})^2}$$
Что и требовалось доказать.

### 3.2 Свойства функций активации

| Свойство | Sigmoid | Tanh |
| :--- | :--- | :--- |
| **Область значений** | $(0, 1)$ | $(-1, 1)$ |
| **Зоны насыщения** | $z \to +\infty$ ($\sigma \to 1$) и $z \to -\infty$ ($\sigma \to 0$) | $z \to +\infty$ ($\sigma \to 1$) и $z \to -\infty$ ($\sigma \to -1$) |
| **Производная ~ 0** | $z \approx \pm 3$ и дальше | $z \approx \pm 2$ и дальше |
| **$max \|\sigma'(z)\|$** | $0.25$ при $z=0$ | $1$ при $z=0$ |

**Почему Tanh предпочтительнее Sigmoid?**
1.  **Нулевое среднее (zero-centered):** Выход Tanh имеет среднее около 0. Это важно, потому что если входы в следующий слой всегда положительны (как у Sigmoid), градиенты весов $W$ будут все одного знака, что приводит к зигзагообразному движению при оптимизации и замедляет сходимость.
2.  **Больший градиент:** Максимальный градиент Tanh равен 1, тогда как у Sigmoid — 0.25. Это позволяет избежать слишком быстрого затухания сигнала ошибки на начальных этапах обратного распространения.

### 3.3 Затухание градиентов в сети Sigmoid + Tanh

Выражение для $\delta^{(1)}$ через $\delta^{(3)}$:
$$\delta^{(1)} = \left( \left( \delta^{(3)} \cdot (W^{(3)})^{\top} \right) \odot \sigma'_{\text{Tanh}}(z^{(2)}) \right) \cdot (W^{(2)})^{\top} \odot \sigma'_{\text{Sigmoid}}(z^{(1)})$$

Оценим норму градиента. Примем $\max|\sigma'_{\text{Tanh}}| = 1$ и $\max|\sigma'_{\text{Sigmoid}}| = 0.25$.

$$\|\delta^{(1)}\| \le \|\delta^{(3)}\| \cdot \|W^{(3)}\| \cdot 1 \cdot \|W^{(2)}\| \cdot 0.25 = 0.25 \cdot \|\delta^{(3)}\| \cdot \|W^{(3)}\| \cdot \|W^{(2)}\|$$

Даже при оптимальной инициализации весов (например, Xavier/Glorot), где $\|W\| \approx 1$, произведение норм весов $\|W^{(3)}\| \cdot \|W^{(2)}\|$ может быть близко к 1.

**Ключевой вывод:** Множитель $0.25$ от производной Sigmoid гарантирует, что на каждом слое, где используется Sigmoid, градиент уменьшается как минимум в 4 раза. В глубокой сети с $L$ слоями Sigmoid градиент будет затухать экспоненциально как $0.25^L$.

**Практический вывод:** Sigmoid непригодна для использования в глубоких сетях (больше 2-3 слоев), так как градиент на первых слоях становится настолько маленьким, что обучение практически останавливается. Tanh работает лучше, но также может страдать от насыщения. Современные решения (ReLU, LeakyReLU) избегают проблемы насыщения в положительной области.
