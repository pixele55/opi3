# Теоретическая часть — ЛР 5
**Студент:** Чернов Дмитрий Андреевич 
**Student ID:** 22  
**Вариант Q3:** 1 (Sigmoid + Tanh)  
**Архитектура:** 784 → 256 → 128 → 10   

---

## Q1. Прямой проход

### 1.1

Прямой проход в матричной форме для батча размера $N$.

**Слой 1** (вход → скрытый, активация Sigmoid):
$$
Z^{(1)} = X W^{(1)} + \mathbf{1}_N b^{(1)\top}, \quad A^{(1)} = \sigma_{\text{sig}}(Z^{(1)})
$$
Размерности:
- $X \in \mathbb{R}^{N \times 784}$ — батч входов
- $W^{(1)} \in \mathbb{R}^{784 \times 256}$, $b^{(1)} \in \mathbb{R}^{256}$
- $Z^{(1)} \in \mathbb{R}^{N \times 256}$ — преактивации
- $A^{(1)} \in \mathbb{R}^{N \times 256}$ — активации

**Слой 2** (скрытый → скрытый, активация Tanh):
$$
Z^{(2)} = A^{(1)} W^{(2)} + \mathbf{1}_N b^{(2)\top}, \quad A^{(2)} = \tanh(Z^{(2)})
$$
Размерности:
- $W^{(2)} \in \mathbb{R}^{256 \times 128}$, $b^{(2)} \in \mathbb{R}^{128}$
- $Z^{(2)}, A^{(2)} \in \mathbb{R}^{N \times 128}$

**Слой 3** (скрытый → выход, активация Softmax):
$$
Z^{(3)} = A^{(2)} W^{(3)} + \mathbf{1}_N b^{(3)\top}, \quad \hat{Y} = \text{softmax}(Z^{(3)})
$$
где softmax применяется построчно:
$$
\hat{y}_{ik} = \frac{\exp(z^{(3)}_{ik})}{\sum_{j=1}^{10} \exp(z^{(3)}_{ij})}
$$
Размерности:
- $W^{(3)} \in \mathbb{R}^{128 \times 10}$, $b^{(3)} \in \mathbb{R}^{10}$
- $Z^{(3)}, \hat{Y} \in \mathbb{R}^{N \times 10}$

Здесь $\mathbf{1}_N \in \mathbb{R}^{N \times 1}$ — вектор-столбец из единиц, обеспечивающий broadcast смещения по батчу.

---

### 1.2

Функция потерь cross-entropy для одного примера $(\mathbf{x}_i, y_i)$:
$$
\mathscr{L}(\hat{y}_i, y_i) = -\sum_{k=1}^{K} y_{ik} \log \hat{y}_{ik}
$$
где $K = 10$ — число классов (для Fashion-MNIST: футболка, штаны, пуловер, платье, пальто, сандалия, рубашка, кроссовок, сумка, ботинок).

Функционал качества — среднее по выборке $X^\ell$ объёма $\ell$:
$$
Q(\boldsymbol{\theta}, X^\ell) = \frac{1}{\ell}\sum_{i=1}^{\ell} \mathscr{L}(\hat{y}_i, y_i)
$$
где $\boldsymbol{\theta} = \{W^{(1)}, b^{(1)}, W^{(2)}, b^{(2)}, W^{(3)}, b^{(3)}\}$ — все обучаемые параметры.

**Роль one-hot кодирования.** Метка $y_i$ — это индекс класса (например, $y_i = 3$ — платье). В one-hot виде $y_i$ превращается в вектор $y_i \in \{0, 1\}^K$, где $y_{ik} = 1$ iff $k = y_i$, иначе $0$. Тогда в сумме $\mathscr{L}$ остаётся ровно одно ненулевое слагаемое:
$$
\mathscr{L}(\hat{y}_i, y_i) = -\log \hat{y}_{i, y_i}
$$
то есть потери — это просто отрицательный логарифм вероятности, присвоенной правильному классу.

**Стремление к нулю при правильной классификации.** Если сеть уверена и предсказывает правильно, то $\hat{y}_{i, y_i} \to 1$, тогда $-\log \hat{y}_{i, y_i} \to -\log 1 = 0$. Если же уверенность низкая ($\hat{y}_{i, y_i} \to 0$), потери стремятся к $+\infty$, что сильно штрафует модель. Таким образом, минимизация $Q$ эквивалентна максимизации правдоподобия (MLE).

---

### 1.3

**Численная нестабильность softmax.** В наивной реализации:
$$
\hat{y}_k = \frac{\exp(z_k)}{\sum_j \exp(z_j)}
$$
Если хотя бы один логит $z_k$ велик по модулю (например, $z_k > 700$), то $\exp(z_k)$ превышает диапазон `float64` и даёт `inf`. Тогда и числитель, и знаменатель становятся `inf`, и результат — `NaN`.

**Стабильная формула.** Вычтем из всех логитов их максимум:
$$
\hat{y}_k = \frac{\exp(z_k - m)}{\sum_j \exp(z_j - m)}, \quad m = \max_j z_j
$$

**Доказательство эквивалентности.** Умножим числитель и знаменатель исходной дроби на $\exp(-m)$:
$$
\frac{\exp(z_k)}{\sum_j \exp(z_j)} = \frac{\exp(z_k) \cdot \exp(-m)}{\sum_j \exp(z_j) \cdot \exp(-m)} = \frac{\exp(z_k - m)}{\sum_j \exp(z_j - m)}
$$
Это алгебраическое тождество, значение не меняется. Но теперь все показатели экспоненты неположительны ($z_k - m \leq 0$), поэтому $\exp(z_k - m) \in (0, 1]$, и переполнения не возникает.

---

## Q2. Обратный проход

### 2.1

Выводим $\frac{\partial Q}{\partial Z^{(3)}}$ для комбинации softmax + cross-entropy.

Функционал по батчу:
$$
Q = \frac{1}{N} \sum_{i=1}^{N} \mathscr{L}_i = -\frac{1}{N} \sum_{i=1}^{N} \sum_{k=1}^{K} y_{ik} \log \hat{y}_{ik}
$$

**Шаг 1.** Производная потерь по одному логиту $z_{ik}$ (индекс $i$ для примера в батче опускаем для краткости):
$$
\frac{\partial \mathscr{L}}{\partial z_k} = -\sum_{j=1}^{K} y_j \frac{\partial \log \hat{y}_j}{\partial z_k} = -\sum_{j=1}^{K} y_j \cdot \frac{1}{\hat{y}_j} \cdot \frac{\partial \hat{y}_j}{\partial z_k}
$$

**Шаг 2.** Производная softmax. Для $j = k$:
$$
\frac{\partial \hat{y}_k}{\partial z_k} = \hat{y}_k (1 - \hat{y}_k)
$$
Для $j \neq k$:
$$
\frac{\partial \hat{y}_j}{\partial z_k} = -\hat{y}_j \hat{y}_k
$$
Объединяя через символ Кронекера:
$$
\frac{\partial \hat{y}_j}{\partial z_k} = \hat{y}_j (\delta_{jk} - \hat{y}_k)
$$

**Шаг 3.** Подставляем:
$$
\frac{\partial \mathscr{L}}{\partial z_k} = -\sum_{j=1}^{K} y_j \cdot \frac{1}{\hat{y}_j} \cdot \hat{y}_j (\delta_{jk} - \hat{y}_k) = -\sum_{j=1}^{K} y_j (\delta_{jk} - \hat{y}_k)
$$
$$
= -\left(y_k - \hat{y}_k \sum_{j=1}^{K} y_j\right)
$$
Поскольку $y$ — one-hot вектор, $\sum_j y_j = 1$, поэтому:
$$
\frac{\partial \mathscr{L}}{\partial z_k} = \hat{y}_k - y_k
$$

**Шаг 4.** Усредняем по батчу:
$$
\frac{\partial Q}{\partial Z^{(3)}} = \frac{1}{N} (\hat{Y} - Y)
$$
где $\hat{Y}, Y \in \mathbb{R}^{N \times K}$.

**Интерпретация.** Градиент — это просто разность предсказанной вероятности и истинной метки. Если сеть уверена неправильно ($\hat{y}_k \ll y_k$), градиент большой и толкает веса в нужную сторону. Если попала в цель ($\hat{y}_k \approx y_k$), градиент малый — обучение замедляется, что и требуется.

---

### 2.2

Обозначим $\Delta^{(3)} = \frac{\partial Q}{\partial Z^{(3)}} \in \mathbb{R}^{N \times 10}$.

**Градиент по $W^{(3)}$.** Из $Z^{(3)} = A^{(2)} W^{(3)} + \mathbf{1} b^{(3)\top}$ по цепному правилу:
$$
\frac{\partial Q}{\partial W^{(3)}} = (A^{(2)})^\top \cdot \Delta^{(3)}
$$
Размерности: $(128 \times N) \cdot (N \times 10) = 128 \times 10$ ✓

**Градиент по $b^{(3)}$.** Смещение добавляется ко всем строкам $Z^{(3)}$ одинаково, поэтому градиент — сумма по батчу:
$$
\frac{\partial Q}{\partial b^{(3)}} = \sum_{i=1}^{N} \Delta^{(3)}_i = \mathbf{1}_N^\top \Delta^{(3)} \in \mathbb{R}^{1 \times 10}
$$
(или $\in \mathbb{R}^{10}$ после squeeze).

**Проверка размерностей:**
- $A^{(2)} \in \mathbb{R}^{N \times 128}$, $(A^{(2)})^\top \in \mathbb{R}^{128 \times N}$
- $\Delta^{(3)} \in \mathbb{R}^{N \times 10}$
- Итог: $128 \times 10$ — совпадает с $W^{(3)}$ ✓

---

### 2.3

Общая формула backprop для скрытого слоя $l$:
$$
\delta^{(l)} = \left(\delta^{(l+1)} \cdot (W^{(l+1)})^\top\right) \odot \sigma'_l(Z^{(l)})
$$

**Содержательное объяснение каждого множителя:**

1. **$\delta^{(l+1)} \in \mathbb{R}^{N \times n_{l+1}}$** — градиент функционала по преактивациям следующего слоя. Содержит информацию о том, «насколько ошибся» слой $l+1$ по каждому нейрону и каждому примеру батча.

2. **$(W^{(l+1)})^\top \in \mathbb{R}^{n_{l+1} \times n_l}$** — транспонированная матрица весов между слоями $l$ и $l+1$. Умножение на неё **распределяет ошибку** с нейронов слоя $l+1$ обратно на нейроны слоя $l$: вклад нейрона $j$ слоя $l$ в ошибку пропорционален весу $W^{(l+1)}_{jk}$, с которым он связан с нейроном $k$ следующего слоя. Транспонирование возникает из-за правил дифференцирования матричных произведений (строки и столбцы меняются местами).

3. **$\odot$ — поэлементное произведение** с $\sigma'_l(Z^{(l)})$. Это производная функции активации, вычисленная в точках преактиваций слоя $l$. Она **масштабирует градиент** в зависимости от того, в каком режиме работал нейрон:
   - Если нейрон был в зоне насыщения ($\sigma' \approx 0$), градиент почти не проходит — нейрон «заблокирован».
   - Если нейрон работал в чувствительной зоне ($\sigma' > 0$), градиент проходит дальше.

Именно множитель $\sigma'_l$ является причиной **проблемы затухающего градиента** в глубоких сетях: при перемножении многих малых чисел градиент экспоненциально убывает.

---

### 2.4

Последовательно выводим $\delta^{(2)}$ и $\delta^{(1)}$ для нашей архитектуры (Sigmoid на слое 1, Tanh на слое 2).

**Для слоя 2** (активация $\tanh$):
$$
\delta^{(2)} = \left(\delta^{(3)} \cdot (W^{(3)})^\top\right) \odot \tanh'(Z^{(2)})
$$
Размерности:
- $\delta^{(3)} \in \mathbb{R}^{N \times 10}$
- $(W^{(3)})^\top \in \mathbb{R}^{10 \times 128}$
- $\delta^{(3)} (W^{(3)})^\top \in \mathbb{R}^{N \times 128}$
- $\tanh'(Z^{(2)}) = 1 - \tanh^2(Z^{(2)}) = 1 - (A^{(2)})^2 \in \mathbb{R}^{N \times 128}$
- Итог: $\delta^{(2)} \in \mathbb{R}^{N \times 128}$ ✓

**Для слоя 1** (активация $\sigma_{\text{sig}}$):
$$
\delta^{(1)} = \left(\delta^{(2)} \cdot (W^{(2)})^\top\right) \odot \sigma'_{\text{sig}}(Z^{(1)})
$$
где $\sigma'_{\text{sig}}(z) = \sigma_{\text{sig}}(z)(1 - \sigma_{\text{sig}}(z)) = A^{(1)} \odot (1 - A^{(1)})$.

Размерности:
- $\delta^{(2)} \in \mathbb{R}^{N \times 128}$
- $(W^{(2)})^\top \in \mathbb{R}^{128 \times 256}$
- $\delta^{(2)} (W^{(2)})^\top \in \mathbb{R}^{N \times 256}$
- $\sigma'_{\text{sig}}(Z^{(1)}) \in \mathbb{R}^{N \times 256}$
- Итог: $\delta^{(1)} \in \mathbb{R}^{N \times 256}$ ✓

**Градиенты по весам и смещениям:**

Для слоя 2:
$$
\frac{\partial Q}{\partial W^{(2)}} = (A^{(1)})^\top \cdot \delta^{(2)} \in \mathbb{R}^{256 \times 128}
$$
$$
\frac{\partial Q}{\partial b^{(2)}} = \mathbf{1}_N^\top \delta^{(2)} \in \mathbb{R}^{128}
$$

Для слоя 1:
$$
\frac{\partial Q}{\partial W^{(1)}} = X^\top \cdot \delta^{(1)} \in \mathbb{R}^{784 \times 256}
$$
$$
\frac{\partial Q}{\partial b^{(1)}} = \mathbf{1}_N^\top \delta^{(1)} \in \mathbb{R}^{256}
$$

Все размерности согласованы с размерностями соответствующих весов.

---

## Q3. Функции активации и затухание градиентов

### 3.1

**Sigmoid:**
$$
\sigma(z) = \frac{1}{1 + e^{-z}}
$$

Производная:
$$
\sigma'(z) = \frac{d}{dz} (1 + e^{-z})^{-1} = -(1 + e^{-z})^{-2} \cdot (-e^{-z}) = \frac{e^{-z}}{(1 + e^{-z})^2}
$$
Преобразуем:
$$
\sigma'(z) = \frac{1}{1 + e^{-z}} \cdot \frac{e^{-z}}{1 + e^{-z}} = \sigma(z) \cdot \frac{(1 + e^{-z}) - 1}{1 + e^{-z}} = \sigma(z)(1 - \sigma(z))
$$
Итак, $\boxed{\sigma'(z) = \sigma(z)(1 - \sigma(z))}$.

**Tanh:**
$$
\tanh(z) = \frac{e^z - e^{-z}}{e^z + e^{-z}} = \frac{e^{2z} - 1}{e^{2z} + 1}
$$

Производная (через правило частного):
$$
\tanh'(z) = \frac{(e^z + e^{-z})(e^z + e^{-z}) - (e^z - e^{-z})(e^z - e^{-z})}{(e^z + e^{-z})^2}
$$
Числитель:
$$
(e^z + e^{-z})^2 - (e^z - e^{-z})^2 = (e^{2z} + 2 + e^{-2z}) - (e^{2z} - 2 + e^{-2z}) = 4
$$
Знаменатель: $(e^z + e^{-z})^2$.

Итак:
$$
\tanh'(z) = \frac{4}{(e^z + e^{-z})^2} = 1 - \left(\frac{e^z - e^{-z}}{e^z + e^{-z}}\right)^2 = 1 - \tanh^2(z)
$$
Итак, $\boxed{\tanh'(z) = 1 - \tanh^2(z)}$.

---

### 3.2

**Sigmoid:**
- Область значений: $(0, 1)$
- Производная: $\sigma'(z) = \sigma(z)(1 - \sigma(z))$. Максимум при $\sigma = 1/2$, т.е. при $z = 0$: $\max \sigma' = 1/4 = 0.25$.
- Зоны насыщения: при $z \to +\infty$ ($\sigma \to 1$) и $z \to -\infty$ ($\sigma \to 0$) производная $\sigma' \to 0$. Практически: $|z| > 5$ — уже насыщение.

**Tanh:**
- Область значений: $(-1, 1)$
- Производная: $\tanh'(z) = 1 - \tanh^2(z)$. Максимум при $\tanh = 0$, т.е. при $z = 0$: $\max \tanh' = 1$.
- Зоны насыщения: при $z \to \pm\infty$ ($\tanh \to \pm 1$) производная $\tanh' \to 0$. Практически: $|z| > 3$ — уже насыщение.

**Сравнение.** Tanh предпочтительнее sigmoid по двум причинам:

1. **Нуль-центрированность.** $\tanh(z) \in (-1, 1)$, среднее значение активаций близко к нулю. Это означает, что градиенты по весам имеют разные знаки и обновление идёт более «зигзагообразно», но эффективнее. У sigmoid все активации положительны ($\in (0, 1)$), что приводит к однонаправленным градиентам и медленной сходимости.

2. **Больше максимальная производная.** $\max \tanh' = 1$, а $\max \sigma' = 0.25$. При обратном распространении градиент умножается на производную активации — у sigmoid он сразу уменьшается в 4 раза даже в лучшей точке, а у tanh — не уменьшается. Это критично в глубоких сетях.

---

### 3.3

В нашей сети: слой 1 — Sigmoid, слой 2 — Tanh. Раскрываем $\delta^{(1)}$ через $\delta^{(3)}$:

$$
\delta^{(2)} = \left(\delta^{(3)} (W^{(3)})^\top\right) \odot \tanh'(Z^{(2)})
$$
$$
\delta^{(1)} = \left(\delta^{(2)} (W^{(2)})^\top\right) \odot \sigma'_{\text{sig}}(Z^{(1)})
$$
Подставляем:
$$
\delta^{(1)} = \left[\left(\delta^{(3)} (W^{(3)})^\top\right) \odot \tanh'(Z^{(2)})\right] (W^{(2)})^\top \odot \sigma'_{\text{sig}}(Z^{(1)})
$$

**Оценка верхней границы.** Используя субмультипликативность нормы и $\max |\sigma'|$:
$$
\|\delta^{(1)}\| \leq \|\delta^{(3)}\| \cdot \|W^{(3)}\| \cdot \max|\tanh'| \cdot \|W^{(2)}\| \cdot \max|\sigma'_{\text{sig}}|
$$
$$
\|\delta^{(1)}\| \leq \|\delta^{(3)}\| \cdot \|W^{(3)}\| \cdot \|W^{(2)}\| \cdot 1 \cdot 0.25
$$

Если предположить, что веса инициализированы так, что $\|W^{(l)}\| \approx 1$ (например, Xavier/He инициализация нормирует дисперсию), то:
$$
\|\delta^{(1)}\| \lesssim 0.25 \cdot \|\delta^{(3)}\|
$$
то есть градиент уже на первом слое **уменьшается минимум в 4 раза** по сравнению с выходным.

**Экспоненциальное затухание в глубоких сетях.** В сети из $L$ слоёв с sigmoid-активациями на каждом:
$$
\|\delta^{(1)}\| \lesssim (0.25)^{L-1} \cdot \|\delta^{(L)}\| \cdot \prod_{l} \|W^{(l)}\|
$$
При $L = 5$ уже $(0.25)^4 \approx 0.004$, при $L = 10$ — $\sim 10^{-6}$. Градиент на первых слоях становится пренебрежимо малым, и их веса почти не обновляются — это и есть **проблема затухающего градиента** (vanishing gradient). Именно поэтому в современных глубоких сетях sigmoid практически не используется, а заменяется на ReLU/LReLU, у которых производная в положительной ветви равна 1 и не убивает градиент.

В нашем варианте (Sigmoid + Tanh) проблема смягчается тем, что tanh даёт $\max = 1$, но sigmoid на первом слое всё равно вносит множитель $0.25$, из-за чего глубокие sigmoid-сети обучать крайне тяжело.
