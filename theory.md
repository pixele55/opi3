# Теоретическая часть — ЛР 5
**Студент:** Чернов Дмитрий Андреевич  
**Student ID:** 22  
**Вариант Q3:** 1 (Sigmoid + Tanh)  
**Архитектура:** 784 → 256 → 128 → 10  

---

## Q1. Прямой проход

### 1.1 Прямой проход многослойного перцептрона

Формулы прямого прохода в матричной форме:
$$z^{(l)} = a^{(l-1)} W^{(l)} + b^{(l)}, \quad a^{(l)} = \sigma_l(z^{(l)})$$

Раскроем их последовательно для слоёв $l = 1, 2, 3$ при обработке батча размера $N$:

**Слой 1 (скрытый, Sigmoid):**
$$z^{(1)} = a^{(0)} W^{(1)} + b^{(1)}$$
$$a^{(1)} = \sigma_{\text{sigmoid}}(z^{(1)}) = \frac{1}{1 + e^{-z^{(1)}}}$$

*Размерности:*
- $a^{(0)} \in \mathbb{R}^{N \times 784}$ (входная матрица признаков батча)
- $W^{(1)} \in \mathbb{R}^{784 \times 256}$ (матрица весов первого слоя)
- $b^{(1)} \in \mathbb{R}^{256}$ (вектор смещений)
- $z^{(1)}, a^{(1)} \in \mathbb{R}^{N \times 256}$

**Слой 2 (скрытый, Tanh):**
$$z^{(2)} = a^{(1)} W^{(2)} + b^{(2)}$$
$$a^{(2)} = \sigma_{\text{tanh}}(z^{(2)}) = \frac{e^{z^{(2)}} - e^{-z^{(2)}}}{e^{z^{(2)}} + e^{-z^{(2)}}}$$

*Размерности:*
- $W^{(2)} \in \mathbb{R}^{256 \times 128}$
- $b^{(2)} \in \mathbb{R}^{128}$
- $z^{(2)}, a^{(2)} \in \mathbb{R}^{N \times 128}$

**Слой 3 (выходной, Softmax):**
$$z^{(3)} = a^{(2)} W^{(3)} + b^{(3)}$$
$$\hat{y}_{ik} = \frac{\exp(z^{(3)}_{ik})}{\sum_{j=1}^{10} \exp(z^{(3)}_{ij})}$$

*Размерности:*
- $W^{(3)} \in \mathbb{R}^{128 \times 10}$
- $b^{(3)} \in \mathbb{R}^{10}$
- $z^{(3)} \in \mathbb{R}^{N \times 10}$
- $\hat{Y} \in \mathbb{R}^{N \times 10}$ (матрица предсказанных вероятностей)

### 1.2 Функция потерь cross-entropy

Функция потерь для одного примера:
$$\mathscr{L}(\hat{y}_i, y_i) = -\sum_{k=1}^{K} y_{ik} \log \hat{y}_{ik}$$

Функционал качества для всей выборки:
$$Q(\boldsymbol{\theta}, X^\ell) = \frac{1}{\ell}\sum_{i=1}^{\ell} \mathscr{L}(\hat{y}_i, y_i)$$

**Роль one-hot кодирования:**
Вектор истинных меток $y_i$ состоит из нулей и одной единицы на позиции правильного класса $c$ (т.е. $y_{ic} = 1$ и $y_{ik} = 0$ для $k \neq c$). Благодаря этому сумма по $k$ схлопывается до одного слагаемого:
$$\mathscr{L}(\hat{y}_i, y_i) = -\log \hat{y}_{ic}$$
Это означает, что штраф зависит исключительно от предсказанной вероятности для истинного класса.

**Стремление к нулю:**
При правильной и уверенной классификации сеть выдает для истинного класса вероятность, близкую к 1 ($\hat{y}_{ic} \to 1$). 
Тогда $\mathscr{L} \to -\log(1) = 0$. Таким образом, чем увереннее и точнее предсказание, тем меньше значение функции потерь.

### 1.3 Численная стабильность softmax

Оригинальная формула softmax:
$$\hat{y}_k = \frac{\exp(z^{(3)}_k)}{\sum_{j=1}^{K} \exp(z^{(3)}_j)}$$

**Проблема нестабильности:**
Если логиты $z^{(3)}_k$ принимают большие положительные значения (например, 1000), вычисление $\exp(1000)$ вызывает арифметическое переполнение (overflow) типа данных с плавающей точкой, что приводит к значениям `Inf` и `NaN`.

**Численно стабильная формула:**
Обозначим $m = \max_j z^{(3)}_j$. Вычтем $m$ из каждого логита:
$$\hat{y}_k = \frac{\exp(z^{(3)}_k - m)}{\sum_{j=1}^{K} \exp(z^{(3)}_j - m)}$$

**Доказательство эквивалентности:**
$$\frac{\exp(z^{(3)}_k - m)}{\sum_{j=1}^{K} \exp(z^{(3)}_j - m)} = \frac{\exp(z^{(3)}_k) \cdot \exp(-m)}{\sum_{j=1}^{K} (\exp(z^{(3)}_j) \cdot \exp(-m))} = \frac{\exp(-m) \cdot \exp(z^{(3)}_k)}{\exp(-m) \cdot \sum_{j=1}^{K} \exp(z^{(3)}_j)} = \frac{\exp(z^{(3)}_k)}{\sum_{j=1}^{K} \exp(z^{(3)}_j)}$$

Поскольку мы вычли максимум $m$, наибольшее значение в степени экспоненты становится равным 0 ($\exp(0)=1$), а остальные — отрицательными. Экспонента от неположительных чисел лежит в диапазоне $(0, 1]$, что полностью исключает переполнение.

---

## Q2. Обратный проход

### 2.1 Градиент функционала по логитам выходного слоя

Необходимо найти $\frac{\partial \mathscr{L}_i}{\partial z^{(3)}_{ij}}$ для одного примера $i$.

Сначала найдем производную softmax $\hat{y}_{ik}$ по $z^{(3)}_{ij}$:

- Если $k = j$: 
  $$\frac{\partial \hat{y}_{ij}}{\partial z^{(3)}_{ij}} = \frac{\exp(z^{(3)}_{ij}) \sum - \exp(z^{(3)}_{ij}) \exp(z^{(3)}_{ij})}{(\sum)^2} = \hat{y}_{ij}(1 - \hat{y}_{ij})$$

- Если $k \neq j$:
  $$\frac{\partial \hat{y}_{ik}}{\partial z^{(3)}_{ij}} = \frac{0 - \exp(z^{(3)}_{ik}) \exp(z^{(3)}_{ij})}{(\sum)^2} = -\hat{y}_{ik} \hat{y}_{ij}$$

Применим цепное правило для $\mathscr{L}_i = -\sum_{k=1}^K y_{ik} \log \hat{y}_{ik}$:
$$\frac{\partial \mathscr{L}_i}{\partial z^{(3)}_{ij}} = \sum_{k=1}^K \frac{\partial \mathscr{L}_i}{\partial \hat{y}_{ik}} \frac{\partial \hat{y}_{ik}}{\partial z^{(3)}_{ij}} = \sum_{k=1}^K \left( -\frac{y_{ik}}{\hat{y}_{ik}} \right) \frac{\partial \hat{y}_{ik}}{\partial z^{(3)}_{ij}}$$

Подставляем производные softmax:
$$\frac{\partial \mathscr{L}_i}{\partial z^{(3)}_{ij}} = -\frac{y_{ij}}{\hat{y}_{ij}} \hat{y}_{ij}(1 - \hat{y}_{ij}) - \sum_{k \neq j} \frac{y_{ik}}{\hat{y}_{ik}} (-\hat{y}_{ik} \hat{y}_{ij})$$
$$= -y_{ij}(1 - \hat{y}_{ij}) + \sum_{k \neq j} y_{ik} \hat{y}_{ij} = -y_{ij} + y_{ij}\hat{y}_{ij} + \sum_{k \neq j} y_{ik} \hat{y}_{ij}$$
$$= -y_{ij} + \hat{y}_{ij} \sum_{k=1}^K y_{ik}$$

Так как $y_i$ — one-hot вектор, сумма его элементов равна 1 ($\sum_{k=1}^K y_{ik} = 1$). Получаем:
$$\frac{\partial \mathscr{L}_i}{\partial z^{(3)}_{ij}} = \hat{y}_{ij} - y_{ij}$$

Усредняя по батчу размера $N$, получаем элегантную матричную форму:
$$\frac{\partial Q}{\partial z^{(3)}} = \frac{1}{N}(\hat{Y} - Y)$$

### 2.2 Градиенты весов и смещений выходного слоя

Формулы:
$$\frac{\partial Q}{\partial W^{(3)}} = (a^{(2)})^\top \cdot \frac{\partial Q}{\partial z^{(3)}}, \quad \frac{\partial Q}{\partial b^{(3)}} = \sum_{i=1}^{N} \frac{\partial Q}{\partial z^{(3)}_i}$$

*Размерности:*
- $a^{(2)} \in \mathbb{R}^{N \times 128} \implies (a^{(2)})^\top \in \mathbb{R}^{128 \times N}$
- $\frac{\partial Q}{\partial z^{(3)}} \in \mathbb{R}^{N \times 10}$
- $\frac{\partial Q}{\partial W^{(3)}} \in \mathbb{R}^{128 \times 10}$ (совпадает с размерностью $W^{(3)}$)
- Сумма $\frac{\partial Q}{\partial z^{(3)}}$ по оси батча $N$ даёт вектор $\frac{\partial Q}{\partial b^{(3)}} \in \mathbb{R}^{10}$ (совпадает с размерностью $b^{(3)}$)

### 2.3 Общая формула обратного распространения ошибки

$$\delta^{(l)} = \left(\delta^{(l+1)} \cdot (W^{(l+1)})^\top\right) \odot \sigma'_l(z^{(l)})$$

**Содержательный смысл множителей:**
- $\delta^{(l+1)}$ — это градиент (ошибка) на следующем слое.
- Умножение на $(W^{(l+1)})^\top$ "пропускает" ошибку обратно через веса, связывающие текущий слой со следующим. Это дает производную функционала по выходам текущего слоя $\frac{\partial Q}{\partial a^{(l)}}$.
- Поэлементное умножение $\odot \sigma'_l(z^{(l)})$ — это применение цепного правила для функции активации текущего слоя ($a^{(l)} = \sigma_l(z^{(l)})$). Оно преобразует градиент по активациям $\frac{\partial Q}{\partial a^{(l)}}$ в градиент по логитам $\frac{\partial Q}{\partial z^{(l)}}$, то есть в $\delta^{(l)}$.

### 2.4 Вывод $\delta$ для слоёв 1 и 2

**Для слоя 2 (Tanh):**
$$\delta^{(2)} = \left(\delta^{(3)} \cdot (W^{(3)})^\top\right) \odot \sigma'_{\text{tanh}}(z^{(2)})$$

Производная Tanh:
$$\sigma'_{\text{tanh}}(z) = 1 - \tanh^2(z) = 1 - (a^{(2)})^2$$

Градиенты:
$$\frac{\partial Q}{\partial W^{(2)}} = (a^{(1)})^\top \cdot \delta^{(2)}, \quad \frac{\partial Q}{\partial b^{(2)}} = \sum_{i=1}^{N} \delta^{(2)}_i$$

**Для слоя 1 (Sigmoid):**
$$\delta^{(1)} = \left(\delta^{(2)} \cdot (W^{(2)})^\top\right) \odot \sigma'_{\text{sigmoid}}(z^{(1)})$$

Производная Sigmoid:
$$\sigma'_{\text{sigmoid}}(z) = \sigma_{\text{sigmoid}}(z)(1 - \sigma_{\text{sigmoid}}(z)) = a^{(1)} \odot (1 - a^{(1)})$$

Градиенты:
$$\frac{\partial Q}{\partial W^{(1)}} = (a^{(0)})^\top \cdot \delta^{(1)}, \quad \frac{\partial Q}{\partial b^{(1)}} = \sum_{i=1}^{N} \delta^{(1)}_i$$

---

## Q3. Функции активации и затухание градиентов

### 3.1 Определения и производные

**Sigmoid:**
$$\sigma_{\text{Sigmoid}}(z) = \frac{1}{1 + e^{-z}}$$

*Производная:*
$$\sigma'_{\text{Sigmoid}}(z) = \frac{d}{dz} (1 + e^{-z})^{-1} = -(1 + e^{-z})^{-2} \cdot (-e^{-z}) = \frac{e^{-z}}{(1 + e^{-z})^2}$$

Заметим, что $e^{-z} = (1 + e^{-z}) - 1$, тогда:
$$\frac{e^{-z}}{(1 + e^{-z})^2} = \frac{1 + e^{-z}}{(1 + e^{-z})^2} - \frac{1}{(1 + e^{-z})^2} = \frac{1}{1 + e^{-z}} - \left(\frac{1}{1 + e^{-z}}\right)^2 = \sigma(z)(1 - \sigma(z))$$

**Tanh:**
$$\sigma_{\text{Tanh}}(z) = \frac{e^{z} - e^{-z}}{e^{z} + e^{-z}}$$

*Производная (доказательство $\tanh'(z) = 1 - \tanh^2(z)$):*
$$\sigma'_{\text{Tanh}}(z) = \frac{(e^{z} + e^{-z})^2 - (e^{z} - e^{-z})^2}{(e^{z} + e^{-z})^2} = \frac{4}{(e^{z} + e^{-z})^2}$$

Выразим через $\sigma_{\text{Tanh}}(z)$:
$$1 - \sigma_{\text{Tanh}}^2(z) = 1 - \left(\frac{e^{z} - e^{-z}}{e^{z} + e^{-z}}\right)^2 = \frac{(e^{z} + e^{-z})^2 - (e^{z} - e^{-z})^2}{(e^{z} + e^{-z})^2} = \frac{4}{(e^{z} + e^{-z})^2}$$

Таким образом, $\sigma'_{\text{Tanh}}(z) = 1 - \tanh^2(z)$.

### 3.2 Свойства функций активации

**Sigmoid:**
- *Область значений:* $(0, 1)$
- *Зоны насыщения:* при больших по модулю $z$ ($|z| \to \infty$) производная стремится к 0.
- *Максимальное значение $|\sigma'(z)|$:* достигается при $z=0$ и равно $\sigma(0)(1-\sigma(0)) = 0.5 \cdot 0.5 = 0.25$.

**Tanh:**
- *Область значений:* $(-1, 1)$
- *Зоны насыщения:* при больших по модулю $z$ ($|z| \to \infty$) производная стремится к 0.
- *Максимальное значение $|\sigma'(z)|$:* достигается при $z=0$ и равно $1 - 0^2 = 1$.

**Сравнение Sigmoid и Tanh:**

Tanh предпочтительнее Sigmoid по двум причинам:

1. **Нулевое среднее (zero-centered):** Выход Tanh имеет среднее около 0. Это важно, потому что если входы в следующий слой всегда положительны (как у Sigmoid), градиенты весов $W$ будут все одного знака, что приводит к зигзагообразному движению при оптимизации и замедляет сходимость.

2. **Больший градиент:** Максимальный градиент Tanh равен 1, тогда как у Sigmoid — 0.25. Это позволяет избежать слишком быстрого затухания сигнала ошибки на начальных этапах обратного распространения.

### 3.3 Затухание градиентов

В трёхслойной сети с Sigmoid (слой 1) и Tanh (слой 2) градиент проходит через два слоя:
$$\delta^{(1)} = \left(\left(\delta^{(3)} \cdot (W^{(3)})^\top\right) \odot \sigma'_{\text{Tanh}}(z^{(2)})\right) \cdot (W^{(2)})^\top \odot \sigma'_{\text{Sigmoid}}(z^{(1)})$$

Оценим норму градиента $\|\delta^{(1)}\|$ относительно $\|\delta^{(3)}\|$:

Норма матрицы при умножении ограничивается произведением норм. 
Учитывая, что $\max|\sigma'_{\text{Sigmoid}}| = 0.25$ и $\max|\sigma'_{\text{Tanh}}| = 1$, получаем:
$$\|\delta^{(2)}\| \le \|\delta^{(3)}\| \cdot \|W^{(3)}\| \cdot 1$$
$$\|\delta^{(1)}\| \le \|\delta^{(2)}\| \cdot \|W^{(2)}\| \cdot 0.25$$

Объединяя неравенства:
$$\|\delta^{(1)}\| \le 0.25 \cdot \|W^{(2)}\| \cdot \|W^{(3)}\| \cdot \|\delta^{(3)}\|$$

**Практический вывод:**

Множитель $0.25$ от Sigmoid гарантирует, что при прохождении через первый слой градиент уменьшается как минимум в 4 раза (при условии, что веса инициализированы корректно и их норма порядка 1).

В сети из $L$ слоёв с Sigmoid градиент убывает экспоненциально с глубиной:
$$\|\delta^{(1)}\| \propto (0.25)^{L} \cdot \|\delta^{(L+1)}\|$$

Слои, расположенные ближе к входу, получают исчезающе малые градиенты и практически не обучаются. Поэтому Sigmoid непригодна для использования в глубоких сетях (больше 2-3 слоев). Tanh работает лучше (множитель 1, а не 0.25), но также может страдать от насыщения. Современные решения (ReLU, LeakyReLU) избегают проблемы насыщения в положительной области.
