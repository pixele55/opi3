# Теоретическая часть — ЛР 2
**Студент:** [Чернов Дмитрий Андреевич]
**Student ID:** 22
**Вариант:** 0 — Combined Cycle Power Plant

---

## Вопрос 1. Градиент функционала качества

### 1. Вывод ∂Q/∂θⱼ

Функционал качества MSE:

$$Q(\\theta, X^{\\ell}) = \\frac{1}{\\ell} \\sum_{i=1}^{\\ell} (\\langle \\theta, x_i \\rangle + \\theta_0 - y_i)^2$$

Берём производную по θⱼ:

$$\\frac{\\partial Q}{\\partial \\theta_j} = \\frac{2}{\\ell} \\sum_{i=1}^{\\ell} (\\langle \\theta, x_i \\rangle + \\theta_0 - y_i) \\cdot x_{i,j}$$

В матричной форме для всего вектора θ:

$$\\nabla Q = \\frac{2}{\\ell} X^T (X\\theta + \\theta_0 - y)$$

### 2. Эквивалентность с добавлением константного признака

Если добавить к каждому объекту признак xᵢ,₀ = 1, то модель становится:

$$\\langle \\theta, x_i \\rangle + \\theta_0 = \\sum_{j=1}^d \\theta_j x_{i,j} + \\theta_0 \\cdot 1 = \\sum_{j=0}^d \\theta_j x_{i,j}$$

где θ₀ становится частью общего вектора θ. Градиент по θ₀ в исходной записи — это частный случай общей формулы при j=0.

### 3. SGD vs полный градиентный спуск

| Аспект | Полный градиентный спуск | SGD |
|--------|-------------------------|-----|
| Точность градиента | Точный градиент по всей выборке | Шумная оценка по одному примеру |
| Скорость на больших данных | Медленно (O(ℓ·d) на шаг) | Быстро (O(d) на шаг) |
| Сходимость | Монотонная | Шумная, но в среднем сходится |

SGD жертвует точностью градиента ради скорости.

---

## Вопрос 2. Регуляризация L1 и L2

### 1. Ridge (L2) и LASSO (L1)

**Ridge (L2):** 
$$R(\theta) = \frac{1}{2} \|\theta\|_2^2 = \frac{1}{2} \sum_{j=1}^d \theta_j^2$$

**LASSO (L1):** 
$$R(\theta) = \|\theta\|_1 = \sum_{j=1}^d |\theta_j|$$

**Дополнительные слагаемые в градиенте:**

- Ridge: добавляется $\tau \cdot \theta_j$
- LASSO: добавляется $\tau \cdot sign(\theta_j)$

### 2. Геометрическая интерпретация

Область допустимых значений для L2 — круг: $\|\theta\|_2^2 \leq t$

Область допустимых значений для L1 — ромб: $\|\theta\|_1 \leq t$

Оптимальное решение находится в точке касания изолиний MSE с этими областями. Ромб имеет углы на осях, поэтому касание часто происходит в точке, где некоторые координаты равны нулю. Круг не имеет углов, поэтому веса уменьшаются, но не обнуляются.

### 3. Предельные случаи

| | $\tau \to 0$ | $\tau \to +\infty$ |
|---|-------|--------|
| **Ridge** | $\theta \to \theta_{OLS}$ | $\theta \to 0$ |
| **LASSO** | $\theta \to \theta_{OLS}$ | $\theta \to 0$ |

### 4. Проблема $sign(0)$

При $\theta_j = 0$ функция $|\theta_j|$ не дифференцируема. На практике используют **субградиент** — любое число из $[-1, 1]$ (часто берут 0).
---

## Вопрос 3. Анализ результатов на своём датасете

### 1. Сравнение SGD и МНК

**Результаты из ноутбука:**
- МНК MAPE = 0.00710
- SGD MAPE = 0.00730

Результаты **не совпали** (SGD дал ошибку на ~2.8% выше). Возможные причины:
- Недостаточное число эпох (использовано 100 эпох)
- Субоптимальный learning rate (α = 0.01)
- Стохастичность SGD (разные случайные перемешивания дают разный результат)

### 2. Узкое место вычисления (XᵀX)⁻¹

Вычисление $(X^T X)^{-1}$ требует $O(d^3)$ операций и $O(d^2)$ памяти. Узким местом становится при $d \\gg \\ell$ или при $d \\gtrsim 10^4$.

Для моего датасета:
- $\\ell = 9568$ объектов
- $d = 4$ признака (или 14 после feature engineering)

Проблема **не актуальна** — матрица $4 \\times 4$ обращается мгновенно.

---

**Вывод:** МНК дал более точное решение, но SGD достаточно близок к нему при правильно подобранных гиперпараметрах.
