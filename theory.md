# Теоретическая часть — Лабораторная работа №2

**Студент:** [Чернов Дмитрий Андреевич]
**Student ID:** 22
**Вариант:** 0 — Combined Cycle Power Plant

---

## Вопрос 1. Градиент функционала качества

### 1. Вывод ∂Q/∂θⱼ

Функционал качества MSE (Mean Squared Error):

$$Q(\\boldsymbol{\\theta}, X^{\\ell}) = \\frac{1}{\\ell} \\sum_{i=1}^{\\ell} \\left( \\langle \\boldsymbol{\\theta}, \\mathbf{x}_i \\rangle + \\theta_0 - y_i \\right)^2$$

где:
- $\\ell$ — количество объектов в обучающей выборке
- $\\mathbf{x}_i$ — вектор признаков $i$-го объекта
- $y_i$ — правильный ответ для $i$-го объекта
- $\\boldsymbol{\\theta} = (\\theta_1, \\ldots, \\theta_d)$ — вектор весов модели
- $\\theta_0$ — смещение (bias, intercept)

**Шаг 1. Применяем цепное правило**

Производная квадрата функции $(u^2)' = 2u \\cdot u'$. Здесь $u = \\langle \\boldsymbol{\\theta}, \\mathbf{x}_i \\rangle + \\theta_0 - y_i$:

$$\\frac{\\partial Q}{\\partial \\theta_j} = \\frac{1}{\\ell} \\sum_{i=1}^{\\ell} 2 \\left( \\langle \\boldsymbol{\\theta}, \\mathbf{x}_i \\rangle + \\theta_0 - y_i \\right) \\cdot \\frac{\\partial}{\\partial \\theta_j} \\left( \\langle \\boldsymbol{\\theta}, \\mathbf{x}_i \\rangle + \\theta_0 - y_i \\right)$$

**Шаг 2. Вычисляем производную скалярного произведения**

Раскрываем скалярное произведение: $\\langle \\boldsymbol{\\theta}, \\mathbf{x}_i \\rangle = \\sum_{k=1}^d \\theta_k x_{ik}$

При дифференцировании по $\\theta_j$ все слагаемые с $k \\neq j$ обнуляются, остаётся только $\\theta_j x_{ij}$:

$$\\frac{\\partial}{\\partial \\theta_j} \\left( \\sum_{k=1}^d \\theta_k x_{ik} + \\theta_0 - y_i \\right) = x_{ij}$$

**Шаг 3. Подставляем результат**

$$\\frac{\\partial Q}{\\partial \\theta_j} = \\frac{2}{\\ell} \\sum_{i=1}^{\\ell} \\left( \\langle \\boldsymbol{\\theta}, \\mathbf{x}_i \\rangle + \\theta_0 - y_i \\right) x_{ij}$$

**Матричная форма записи**

Обозначим:
- $X$ — матрица признаков размера $\\ell \\times d$
- $\\mathbf{y}$ — вектор ответов размера $\\ell$
- $\\mathbf{e} = X\\boldsymbol{\\theta} + \\theta_0\\mathbf{1} - \\mathbf{y}$ — вектор ошибок

Тогда градиент по всему вектору весов $\\boldsymbol{\\theta}$:

$$\\nabla_{\\boldsymbol{\\theta}} Q = \\frac{2}{\\ell} X^T (X\\boldsymbol{\\theta} + \\theta_0\\mathbf{1} - \\mathbf{y})$$

### 2. Эквивалентность с добавлением константного признака

Добавим к каждому объекту фиктивный признак $x_{i,0} = 1$. Тогда расширенный вектор признаков:

$$\\mathbf{x}_{i,aug} = (1, x_{i1}, x_{i2}, \\ldots, x_{id})$$

Расширенный вектор весов:

$$\\boldsymbol{\\theta}_{aug} = (\\theta_0, \\theta_1, \\ldots, \\theta_d)$$

Скалярное произведение в расширенном пространстве:

$$\\langle \\boldsymbol{\\theta}_{aug}, \\mathbf{x}_{i,aug} \\rangle = \\theta_0 \\cdot 1 + \\sum_{j=1}^d \\theta_j x_{ij} = \\langle \\boldsymbol{\\theta}, \\mathbf{x}_i \\rangle + \\theta_0$$

Таким образом, **смещение $\\theta_0$ становится просто ещё одним весом**, соответствующим признаку, который всегда равен 1.

**Градиент по $\\theta_0$** получаем как частный случай общей формулы при $j=0$, где $x_{i,0} = 1$:

$$\\frac{\\partial Q}{\\partial \\theta_0} = \\frac{2}{\\ell} \\sum_{i=1}^{\\ell} \\left( \\langle \\boldsymbol{\\theta}, \\mathbf{x}_i \\rangle + \\theta_0 - y_i \\right)$$

Это подтверждает, что обе формы записи эквивалентны.

### 3. Сравнение SGD и полного градиентного спуска

| Характеристика | Полный градиентный спуск (BGD) | Стохастический градиентный спуск (SGD) |
|----------------|-------------------------------|---------------------------------------|
| **Градиент** | Точный (по всей выборке) | Шумный (по одному объекту) |
| **Сложность шага** | $O(\\ell \\cdot d)$ | $O(d)$ |
| **Сходимость** | Монотонная | Колеблющаяся |
| **Локальные минимумы** | Застревает | Может "выпрыгнуть" |
| **Использование памяти** | $O(\\ell \\cdot d)$ | $O(d)$ |
| **Применение** | Малые данные ($\\ell < 10^5$) | Большие данные ($\\ell \\gg 10^5$) |

**Что теряется в SGD:**
- **Точность направления** — градиент на одном примере является зашумленной оценкой истинного градиента
- **Монотонность сходимости** — ошибка может временно возрастать
- **Гарантия достижения минимума** — сходится только в среднем

**Что приобретается в SGD:**
- **Скорость** — один шаг требует $O(d)$ вместо $O(\\ell \\cdot d)$
- **Возможность работать с большими данными** — не нужно хранить всю выборку в памяти
- **Способность "выпрыгивать" из локальных минимумов** — шум помогает исследовать пространство параметров

**Вывод:** SGD жертвует точностью градиента ради скорости и масштабируемости.

---

## Вопрос 2. Регуляризация L1 и L2

### 1. Функции регуляризации и дополнительные слагаемые в градиенте

Регуляризованный функционал качества:

$$Q_{\\tau}(\\theta, X^{\\ell}) = Q(\\theta, X^{\\ell}) + \\tau \\cdot R(\\theta)$$

где $\\tau > 0$ — коэффициент регуляризации.

**Ridge (L2-регуляризация):**

$$R(\\theta) = \\frac{1}{2} \\|\\boldsymbol{\\theta}\\|_2^2 = \\frac{1}{2} \\sum_{j=1}^d \\theta_j^2$$

Производная регуляризационного члена по $\\theta_j$:

$$\\frac{\\partial}{\\partial \\theta_j} \\left( \\frac{\\tau}{2} \\theta_j^2 \\right) = \\tau \\theta_j$$

**Дополнительное слагаемое в градиенте:** $+ \\tau \\theta_j$

**LASSO (L1-регуляризация):**

$$R(\\theta) = \\|\\boldsymbol{\\theta}\\|_1 = \\sum_{j=1}^d |\\theta_j|$$

Производная (субградиент) по $\\theta_j$:

$$\\frac{\\partial}{\\partial \\theta_j} (\\tau |\\theta_j|) = \\tau \\cdot \\text{sign}(\\theta_j)$$

где $\\text{sign}(\\theta_j) = \\begin{cases} 1, & \\theta_j > 0 \\\\ -1, & \\theta_j < 0 \\end{cases}$

**Дополнительное слагаемое в градиенте:** $+ \\tau \\cdot \\text{sign}(\\theta_j)$

### 2. Геометрическая интерпретация

Мы минимизируем $Q(\\theta)$ при ограничении $R(\\theta) \\le t$.

**L2 (Ridge):** Область допустимых значений $\\|\\theta\\|_2^2 \\le t$ — это **круг** (в 2D) или **сфера** (в многомерном пространстве). У круга нет острых углов. Изолинии функции потерь $Q(\\theta)$ (обычно эллипсы) с большой вероятностью коснутся границы круга в точке, где **ни одна координата не равна нулю**. Поэтому веса просто уменьшаются, но не обнуляются.

**L1 (LASSO):** Область допустимых значений $\\|\\theta\\|_1 \\le t$ — это **ромб** (в 2D) или **октаэдр** (в многомерном пространстве). У ромба есть **острые углы (вершины)**, лежащие на осях координат. Вероятность того, что эллипс изолиний коснётся ромба именно в вершине (где одна из координат равна 0), очень высока. Поэтому **LASSO обнуляет неважные признаки**, делая модель разреженной.

**Визуализация (для двух весов $\\theta_1, \\theta_2$):**

- L2: область $\\theta_1^2 + \\theta_2^2 \\le t$ — круг
- L1: область $|\\theta_1| + |\\theta_2| \\le t$ — ромб с вершинами на осях

### 3. Предельные случаи коэффициента регуляризации $\\tau$

| $\\tau$ | Ridge (L2) | LASSO (L1) |
|---------|------------|------------|
| $\\tau \\to 0$ | $\\theta \\to \\theta_{МНК}$ (решение без регуляризации) | $\\theta \\to \\theta_{МНК}$ |
| $\\tau \\to +\\infty$ | $\\theta \\to 0$ (все веса стремятся к нулю) | $\\theta \\to 0$ (все веса стремятся к нулю) |

**Объяснение:**
- При $\\tau \\to 0$ регуляризация исчезает, решение стремится к обычному МНК
- При $\\tau \\to +\\infty$ штраф за ненулевые веса становится бесконечно большим, поэтому оптимальные веса стремятся к нулю

### 4. Проблема $\\text{sign}(0)$ в LASSO

Функция $|\\theta_j|$ **не дифференцируема** в точке $\\theta_j = 0$ (график имеет излом). В этой точке производная не определена единственным образом.

**Как решается на практике:**

1. **Субградиентный метод** — в точке 0 в качестве градиента можно взять любое число из отрезка $[-1, 1]$. Чаще всего берут 0.

2. **Проксимальный оператор (Soft Thresholding)** — вместо обычного шага градиента используется оператор мягкого порога, который аналитически решает задачу минимизации с L1-штрафом:

$$\\text{soft}(w, \\tau) = \\text{sign}(w) \\cdot \\max(|w| - \\tau, 0)$$

Этот оператор зануляет веса, модуль которых меньше $\\tau$.

---

## Вопрос 3. Анализ результатов на своём датасете

**Вариант 0 — Combined Cycle Power Plant**

### 1. Сравнение результатов SGD и МНК

**Результаты из ноутбука:**
- МНК: MAPE = 0.00710 (0.71%)
- SGD (100 эпох, lr=0.01): MAPE = 0.00730 (0.73%)

SGD дал ошибку на ~2.8% выше, чем МНК.

**Причины расхождения:**
- Недостаточное число эпох (100 может быть мало для полной сходимости)
- Субоптимальный learning rate
- Стохастичность SGD — шумная оценка градиента
- МНК даёт точное глобальное решение, SGD только приближается

### 2. Узкое место вычисления $(X^T X)^{-1}$

Вычисление $(X^T X)^{-1}$ требует $O(d^3)$ операций. Проблема возникает при $d \\gg \\ell$ или $d \\gtrsim 10^4$.

Для моего датасета:
- $\\ell = 9568$ объектов
- $d = 4$ (или 14 после feature engineering)

**Вывод:** проблема не актуальна — матрица $14 \\times 14$ обращается мгновенно.
